export default function Settings() {
  return (
    <div className="p-8">
      <h2 className="text-lg font-semibold text-zinc-800">Settings</h2>
      <p className="mt-1 text-sm text-zinc-500">
        Service configuration, save / load — coming in Phase 6.
      </p>
    </div>
  )
}
